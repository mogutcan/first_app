<!DOCTYPE html>
<html>
<head>
  <title><%= baslik( yield(:title)) %> </title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  
  <!-- _shim.html.erb icine tarayıcı uyumsuzlugu yazılır !-->

  <%= render 'layouts/shim' %>

</head>
<body>
  <%= render 'layouts/header' %>   
  <!-- SAYFA BASLIKLARINI layouts/_header ' dan CEKECEK!-->


  <div class = "container">

    <% flash.each do |key, value| %>
      <div class = "alert alert-<%= key %>"> <%= value %> </div>
    <% end %>
           
        <%= yield %>

          <!-- footer sayfanın altında olması için yield altına render ettik !-->
        <%= render 'layouts/footer' %>
        <%= debug(params) if Rails.env.development? %>
   </div>

</body>
</html>
